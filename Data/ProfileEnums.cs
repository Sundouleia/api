namespace SundouleiaAPI.Enums;

public enum PublicityScope
{
    Private,
    PairsOnly,
    Anyone,
}

public enum PlateElement
{
    Plate,
    Avatar,
    Description,
}

public enum StyleKind
{
    Background,
    Border,
    Overlay,
}

// 5 for each
public enum PlateBG
{
    Default,
    Marble,
    Wood,
    Chains,
    Office,
    Angelic,
    Demonic,
}

// 7 each
public enum PlateBorder
{
    Default,
    Marble,
    Wood,
    Chains,
    Office,
    Angelic,
    Demonic,
}

// 6 each
public enum PlateOverlay
{
    Default,
    Marble,
    Wood,
    Chains,
    Office,
    Angelic,
    Demonic,
}